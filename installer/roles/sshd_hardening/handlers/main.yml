---
# =============================================================================
# GWI POS — sshd_hardening handlers
# =============================================================================

- name: restart sshd
  ansible.builtin.systemd:
    name: sshd
    state: reloaded
    daemon_reload: true

- name: restart fail2ban
  ansible.builtin.systemd:
    name: fail2ban
    state: restarted
